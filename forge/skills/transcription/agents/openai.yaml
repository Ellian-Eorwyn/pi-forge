interface:
  display_name: "Transcription"
  short_description: "Transcribe audio or video, then correct and clean it"
  default_prompt: "Use $transcription to transcribe this recording on the llm-stack transcription service, apply the user correction dictionary, and route it into transcript cleanup."
