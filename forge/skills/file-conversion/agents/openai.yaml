interface:
  display_name: "File Conversion"
  short_description: "Convert files, including Markdown and EPUB"
  default_prompt: "Use $file-conversion to convert these files to the requested format with originals preserved."
