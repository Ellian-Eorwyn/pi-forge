interface:
  display_name: "Project Extraction"
  short_description: "Search and refresh live project controls"
  default_prompt: "Use $project-extraction to check the project Inbox, search existing source-backed controls and documents, and refresh deliverables, requirements, dates, decisions, risks, focused views, and Gantt outputs."
