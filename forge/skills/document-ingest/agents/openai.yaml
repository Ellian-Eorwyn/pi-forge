interface:
  display_name: "Document Ingest"
  short_description: "Normalize documents and email with provenance"
  default_prompt: "Use $document-ingest to ingest these documents or EML emails into normalized, provenance-backed outputs."
